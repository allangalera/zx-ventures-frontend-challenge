export const Button = (theme: any) => ({
  color: 'white',
  background: 'black',
  border: 'none',
  borderRadius: '0.5rem',
  padding: '1rem 2rem',
});

export const ButtonSpan = (theme: any) => ({});
