import React from 'react';
import * as styles from './styles';

type Props = {};

const Input: React.FunctionComponent<Props> = (): JSX.Element => {
  return <div css={styles.Input}>Input</div>;
};

export default Input;
