import React from 'react';
import * as styles from './styles';

type Props = {};

const SearchBox: React.FunctionComponent<Props> = (): JSX.Element => {
  return <div css={styles.SearchBox}>SearchBox</div>;
};

export default SearchBox;
